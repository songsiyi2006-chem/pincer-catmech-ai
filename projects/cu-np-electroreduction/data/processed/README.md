# Processed evidence

Canonical processed results live in `../../results/pilot_summary.json` and
`../../results/process_scenarios.csv`. This directory is reserved for future
source-matched physical labels; none exist yet. A test fixture or an assumed
engineering scenario must never be copied here as experimental/DFT truth.
